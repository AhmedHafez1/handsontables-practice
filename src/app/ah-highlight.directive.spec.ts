import { AhHighlightDirective } from './ah-highlight.directive';

describe('AhHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new AhHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
