import { AhColorClickDirective } from './ah-color-clic.directive';

describe('AhColorClicDirective', () => {
  it('should create an instance', () => {
    const directive = new AhColorClickDirective();
    expect(directive).toBeTruthy();
  });
});
