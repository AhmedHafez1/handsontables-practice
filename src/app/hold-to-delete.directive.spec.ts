import { HoldToDeleteDirective } from './hold-to-delete.directive';

describe('HoldToDeleteDirective', () => {
  it('should create an instance', () => {
    const directive = new HoldToDeleteDirective();
    expect(directive).toBeTruthy();
  });
});
