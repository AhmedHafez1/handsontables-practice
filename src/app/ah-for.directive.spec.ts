import { AhForDirective } from './ah-for.directive';

describe('AhForDirective', () => {
  it('should create an instance', () => {
    const directive = new AhForDirective();
    expect(directive).toBeTruthy();
  });
});
